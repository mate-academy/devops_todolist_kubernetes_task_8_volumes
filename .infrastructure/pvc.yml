apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pvcdata
  namespace: todoapp
spec:
  resources:
    requests:
      storage:
  volumeMode: Filesystem
  accessModes:
    - ReadWriteMany
  storageClassName: standard