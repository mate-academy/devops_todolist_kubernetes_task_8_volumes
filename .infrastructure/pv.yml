apiVersion: v1
kind: PersistentVolume
metadata:
  name: pv-todoapp
  namespace: todoapp
spec:
  storegeClassName: standart
  persistentVolumeReclaimPolicy: Delete
  accessModes:
    - ReadWriteMany
  capacity:
    storege: 1Gi
  hostPath:
    path: app/data/
